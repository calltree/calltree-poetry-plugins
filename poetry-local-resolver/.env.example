# AWS CodeArtifact Configuration
# Copy this to .env and update with your values

# Required: CodeArtifact domain name
CODEARTIFACT_DOMAIN=your-domain

# Required: CodeArtifact repository name
CODEARTIFACT_REPOSITORY=your-repository

# Required: AWS Account ID that owns the CodeArtifact domain
CODEARTIFACT_ACCOUNT_ID=123456789012

# Optional: AWS Region (defaults to us-east-1)
AWS_REGION=us-east-1

# Optional: AWS Profile name (leave empty to use default profile)
AWS_PROFILE=your-profile-name